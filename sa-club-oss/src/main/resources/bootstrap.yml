spring:
  application:
    name: sa-club-oss
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: *** #git提交做隐藏
      config:
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
      discovery:
        enabled: true